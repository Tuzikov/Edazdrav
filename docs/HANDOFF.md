# Хендофф — состояние проекта на 21 сентября 2026

Точка передачи между сессиями Claude Code (заканчивается контекст). Новая
сессия: прочитай этот файл + [architecture.drawio](architecture.drawio)
(открыть в app.diagrams.net или через VS Code-расширение Draw.io
Integration) — этого достаточно, чтобы понять, что уже сделано и что дальше.

## Что это за проект
Android-приложение подсчёта калорий по фото (React Native / Expo).
Бриф — [PROJECT_SPEC.md](../PROJECT_SPEC.md). Название и иконка — «Едазавр»
(T-Rex из еды).

## Незакрытое прямо сейчас — проверить в первую очередь
1. **EAS-сборка APK может быть ещё не готова.** В момент хендоффа шла
   сборка коммита `8ec950d` (клавиатура + учащённый Supabase keep-alive).
   Проверить: `npx eas-cli build:list --limit 1` или зайти на
   https://expo.dev/accounts/tuzikov/projects/Edazdrav/builds — если готова,
   просто отдать ссылку пользователю; если нет — она либо ещё в очереди,
   либо уже упала (посмотреть лог). Пересобрать в любом случае можно так:
   `npx eas-cli build -p android --profile preview --non-interactive`.
2. **Пользователь должен подтвердить на реальном устройстве**, что при
   ручном добавлении ингредиента поле ввода больше не перекрывается
   клавиатурой (это был баг, чинили дважды — см. «На заметку» ниже, там
   разобрано почему первая попытка не сработала).
3. **Supabase-проект только что восстановлен после паузы** (был `INACTIVE`,
   пользователь нажал «Restore project» в дашборде, сейчас
   `ACTIVE_HEALTHY` — см. `supabase projects list`). Стоит попросить
   пользователя ещё раз проверить, что распознавание фото в приложении
   реально заработало (Edge Functions могли не сразу подняться после
   restore).

## Текущее состояние — MVP полностью рабочий
- Вкладки Сегодня / История / Профиль, flow добавления блюда
  (фото / галерея / вручную), экран деталей и редактирования уже
  сохранённых приёмов.
- Реальный бэкенд: Supabase Edge Functions (`recognize-meal`,
  `lookup-nutrients`, `lookup-ingredient`, `nutrition-advice` — см. ниже) —
  дёргают Gemini (распознавание фото + перевод названий + рекомендации) и
  USDA FoodData Central (нутриенты). Gemini — платный тир.
- Калькулятор целей (формула Миффлина-Сан Жеора) в Профиле, плюс ручной
  ввод целей.
- **История**: факт/план по калориям и выбранному нутриенту в шапке
  каждого дня; счётчик «N дней в режиме 👍»; **сводка за произвольный
  период** — свой календарь (`src/components/period-calendar.tsx`, без
  внешних зависимостей, тап-тап выбор диапазона) показывает итог и среднее
  в день по калориям+БЖУК против плана; кнопка «Получить рекомендации»
  отправляет эту сводку в новую Edge Function `nutrition-advice`, которая
  просит у Gemini текстовые советы по рациону/активности — **только по
  нажатию кнопки**, не автоматически.
- **MealEditor** (общий компонент для нового блюда и редактирования уже
  сохранённого): у каждого ингредиента теперь подписаны калории (`≈N ккал`,
  живо пересчитывается при изменении граммовки); при ручном добавлении
  ингредиента список скроллится к полю ввода по реальному событию
  появления клавиатуры (`Keyboard.addListener('keyboardDidShow', ...)`),
  а не по таймеру/кадру — см. «На заметку», это был не самый очевидный фикс.
- Кольцо калорий на «Сегодня» краснеет при перерасходе.
- Android: edge-to-edge поддержан (`react-native-edge-to-edge` +
  `useSafeAreaInsets`), иконка приложения и adaptive icon — с отступами под
  безопасную зону маски, без обрезки.
- Иконка вкладки «Профиль» — контурный T-Rex (`assets/images/profile-tab-icon*.png`,
  тонируется через `tintColor`), заменил стандартный `person-outline`.
- **Редизайн «Сочный»** — тёплая оранжевая палитра вместо чёрно-белой,
  токены в `src/constants/theme.ts` (`accent`, `accentButton`, `danger`,
  `nutrientProtein/Fat/Carbs/Fiber`, light+dark). Применено по всему
  приложению.
- **Синхронизация с Health Connect** (Android-only, односторонняя —
  приложение только пишет, ничего не читает обратно) — тумблер в Профиле,
  выключен по умолчанию. Подтверждено пользователем работающим на реальном
  устройстве (Android 16, Samsung) после нескольких раундов отладки — см.
  «На заметку» ниже.
- **Переключатель темы** (Системная/Светлая/Тёмная) в Профиле —
  `ThemePreferenceProvider` + `useResolvedColorScheme()`
  (`src/context/theme-preference-context.tsx`,
  `src/hooks/use-resolved-color-scheme.ts`), выбор сохраняется в
  AsyncStorage, применяется везде (контент, таб-бар, статус-бар через
  `SystemBars`).
- Последний собранный и подтверждённый пользователем на реальном устройстве
  APK — коммит `1069457` (редизайн + Health Connect + тема; на момент этого
  хендоффа собирается **новый** APK с коммита `8ec950d`, см. пункт 1 выше).
  Пересобрать: `npx eas-cli build -p android --profile preview --non-interactive`.

## Доступы и куда что задеплоено
- **GitHub**: github.com/Tuzikov/Edazdrav, ветка `main`, рабочее дерево
  чистое (кроме `docs/Smile.png` и `docs/logo profile.png`, см. ниже).
  Паттерн для новых фич — feature-ветка + PR (`gh pr create` /
  `gh pr merge --merge --delete-branch`), не прямые коммиты в `main`.
- **Supabase** проект `iwqabjqsrqvmqciikzqn` — залогинен локально через
  `supabase login`. Секреты (`GEMINI_API_KEY`, `FDC_API_KEY`,
  `GEMINI_MODEL`) установлены через `supabase secrets set`, деплоить
  функции: `supabase functions deploy <имя>`. **Проект на бесплатном
  тарифе и подвержен автопаузе по неактивности** — см. «На заметку»,
  большой раздел про keep-alive.
- **EAS/Expo** проект `@tuzikov/Edazdrav` (аккаунт tuzikov, `eas login`
  выполнен локально). Профиль сборки `preview` → APK (`buildType: apk` в
  `eas.json`).
- `.env` в корне репозитория (не в git) — `EXPO_PUBLIC_SUPABASE_URL` /
  `EXPO_PUBLIC_SUPABASE_ANON_KEY`. Не секреты, продублированы в `eas.json`
  для облачных сборок.
- Оригинальный Node.js-прототип пайплайна — в `../Edazdrav pre_project/`,
  самим приложением больше не используется.

## Незавершённые задачи
1. **`docs/Smile.png`** (1024×1024, лежит в репо, не закоммичен) —
   пользователь добавил в папку, но не объяснил назначение. Не трогать,
   спросить при случае.

## На заметку (грабли, на которые уже наступали)

### Supabase keep-alive — паузился повторно, пинг был слишком редким
Несмотря на воркфлоу `.github/workflows/keep-alive.yml` (добавлен ещё в
августе), проект **снова ушёл в паузу** в сентябре. История запусков
показала: пинг `2026-09-14` получил живой ответ `200` от базы, а
следующий по расписанию (`2026-09-17`, через 3 дня) упал с
`curl: (6) Could not resolve host` — проект был уже полностью выгружен, то
есть заметно раньше формального 7-дневного порога бездействия. Расписание
было `пн и чт` (2 раза в неделю) — судя по всему, недостаточно часто:
документация Supabase формулирует порог расплывчато («обычно достаточно
нескольких запросов к БД **в день** на протяжении недели»), то есть их
реальная эвристика активности жёстче буквальных «7 дней» без запросов.
**Исправлено**: расписание пинга увеличено до раза в 6 часов (`0 */6 * *
*`). Если и это не поможет — единственный по-настоящему надёжный вариант
это платный тариф Supabase (Pro, там паузы по неактивности нет вообще);
пинг любой частоты — это обход, а не гарантия.

**Важно**: разбудить уже приостановленный проект **нельзя** ни через
`supabase` CLI (проверено — там только `list/create/api-keys/delete`, ни
`restore`, ни `resume` нет), ни через какой-либо API — только кнопка
«Restore project»/«Resume» в Supabase Dashboard
(https://supabase.com/dashboard/project/iwqabjqsrqvmqciikzqn), и нажать
её может только сам пользователь (залогиненный человек), не агент.

### Клавиатура перекрывала поле ввода ингредиента — потребовалось два захода
Первая попытка чинить (коммит `a0e18a7`) скроллила `FlatList` на
`requestAnimationFrame` при фокусе поля — не сработало на реальном
устройстве: один кадр (~16мс) — ничтожно мало по сравнению с длительностью
анимации появления клавиатуры на Android и связанного с ней ресайза окна
(`windowSoftInputMode="adjustResize"`, уже стоит в манифесте). Скролл
срабатывал против ещё не сжавшегося лэйаута. **Исправлено** (коммит
`ee489db`): подписка на `Keyboard.addListener('keyboardDidShow', ...)` —
скроллим только когда ОС подтвердила, что клавиатура реально открылась.
Урок: для авто-скролла к полю ввода под клавиатурой в RN не полагаться на
таймауты/кадры — только на реальное событие.

### Health Connect — целая сага граблей, если будешь трогать эту часть
1. Конфиг-плагин `react-native-health-connect` регистрирует только
   экран-обоснование разрешений в манифесте — сам
   `<uses-permission android:name="android.permission.health.WRITE_NUTRITION"/>`
   нужно объявлять вручную через `android.permissions` в `app.json` (как
   `RECORD_AUDIO`). Без этого Android вообще не показывает системный
   диалог разрешения.
2. В самой библиотеке (`node_modules/react-native-health-connect`, метод
   `requestPermission` в `HealthConnectManager.kt`) нет `try/catch` вокруг
   нативного вызова — единственный метод в файле без него. Любая нативная
   ошибка там просто проглатывалась вместо того, чтобы отклонить promise.
   Пропатчено через `patch-package`
   (`patches/react-native-health-connect+4.1.3.patch`, см. `postinstall` в
   `package.json`).
3. Финальная причина, почему разрешение выдавалось, но данные не
   записывались: `NutritionRecord` — это `IntervalRecord`, Health Connect
   требует `startTime < endTime` строго. Приём пищи в модели приложения —
   мгновенное событие, поэтому все записи падали с `startTime must be
   before endTime`. Исправлено в `src/lib/health-connect.ts` —
   `endTime = startTime + 1 минута`.
4. Нативный `healthConnectClient` живёт только в рамках текущего процесса
   приложения — после перезапуска приложения он снова не инициализирован,
   даже если разрешение уже выдано и тумблер включён. Исправлено —
   `ensureInitialized()` вызывается перед каждой операцией записи/удаления,
   а не один раз при включении тумблера.
5. `src/lib/health-connect.ts` хранит последнюю ошибку синхронизации в
   AsyncStorage (`getLastSyncError`) и показывает её в Профиле под
   тумблером — полезно для будущей отладки, специально не убирал после
   фикса.
6. Подтверждено на реальном устройстве (Android 16, Samsung).

### Остальное
- USDA FoodData Central API периодически отдаёт 400 без видимой причины —
  в `supabase/functions/_shared/usda.ts` есть ретрай (до 4 попыток).
- Общий фолбэк по нутриентам (`GENERIC_FALLBACK` там же) срабатывает, если
  USDA вообще не находит совпадение — может давать неточные цифры для
  редких продуктов.
- Промпт для Gemini явно разделяет "твёрдая еда" и "напитки/жидкости" —
  раньше "вода, raw" уводила поиск USDA не в ту сторону.
- Фото в истории хранятся как временная cache-ссылка от
  `expo-image-picker` — при очистке кэша Android картинка может перестать
  открываться, данные о калориях не пострадают. Договорились пока не
  чинить.
- Локальный `~/.npm` кэш один раз был испорчен root-owned файлами — если
  `npm install` падает с `EACCES`: `sudo chown -R $(whoami):staff ~/.npm`.
- На macOS для растровых операций с иконками — `sips` (встроен) и Pillow
  (`pip3 install Pillow`).

## Как продолжать в новой сессии
Открыть эту папку и сказать «продолжаем, дочитай `docs/HANDOFF.md`».
Начни с раздела «Незакрытое прямо сейчас» — там то, что нужно проверить
в первую очередь.
